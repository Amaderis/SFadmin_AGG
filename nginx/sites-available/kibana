#server {
#    listen 80;
#    server_name 158.160.96.25;
#
#    location /kibana {
#      #  auth_basic "Restricted Access";
##        auth_basic_user_file /etc/nginx/.htpasswd;
#
#        proxy_pass http://localhost:5601/;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#
        # Если вы используете Kibana с SSL, разкомментируйте следующие строки
        # и укажите путь к вашим сертификатам
        #proxy_ssl_certificate /path/to/cert.crt;
        #proxy_ssl_certificate_key /path/to/key.key;
#    }
#}
